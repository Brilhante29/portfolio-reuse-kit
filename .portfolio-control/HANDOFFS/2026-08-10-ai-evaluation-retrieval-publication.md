# AI Evaluation and Retrieval Publication

Six repositories were advanced to `main` without force and passed GitHub Actions on their exact final SHA: #2, #3, #8, #9, #10, and #30.

The system edge `rag-knowledge-base -> llm-eval-harness` is pinned by repository SHA and `prediction-artifact/1.0`. The remaining repositories share evidence conventions, not hidden runtime coupling.

Reusable delta: prediction artifact schema, producer lock schema/template, local-first AI evidence skill, model drift gates, and cross-platform PowerShell native-process handling.
